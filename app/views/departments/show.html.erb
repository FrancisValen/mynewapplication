

<h3>Department: <%= @department.name%> | Location: <%= @department.location%> | Student Count: <%= @department.student_count%> | Employee Count: <%= @department.employee_count%> </h3>


<h3>List of Students:</h3>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Program</th>
      <th>Year Level</th>
    </tr>
  </thead>
  <tbody>
    <% @department.students.each do |student| %>
      <tr>
        <td><%= student.name %></td>
        <td><%= student.program %></td>
        <td><%= student.year_level %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<div>
  <%= link_to "Edit this department", edit_department_path(@department) %> |
  <%= link_to "Back to departments", departments_path %>

  <%= button_to "Destroy this department", @department, method: :delete %>
</div>